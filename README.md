# vue-modal

```
npm i -g @vue/cli
mkdir vue-modal
cd vue-modal
vue create .
npm install sass-loader node-sass --save-dev
npm install --save core-js 
npm run serve
npm run build *
npm run build-lib
```

